const 
        paragraphElement = ((name, innerHTML) => {
            const element = (() => {
                if(Data.includes(name)) return document.createElement(name);
                return new (eval(name)); 
            })();
             if(typeof innerHTML == "string") element.innerHTML = innerHTML;
             else element.append(innerHTML);
            element.innerHTML = innerHTML
            return element;
        })('P', 'A paragraph of text.')
        ;

//*
const 
        DivElement = ((name, innerHTML) => {
            const element = (() => {
                if(Data.includes(name)) return document.createElement(name);
                return new (eval(name)); 
            })();
             if(typeof innerHTML == "string") element.innerHTML = innerHTML;
             else element.append(innerHTML);
            element.innerHTML = innerHTML
            return element;
        })('Div', 
         ((name, innerHTML) => {
            const element = (() => {
                if(Data.includes(name)) return document.createElement(name);
                return new (eval(name)); 
            })();
             if(typeof innerHTML == "string") element.innerHTML = innerHTML;
             else element.append(innerHTML);
            element.innerHTML = innerHTML
            return element;
        })('Div', 1-'hello')
        1-
)
        ;
//*

//:

const 
        SpanElement = ((name, innerHTML) => {
            const element = (() => {
                if(Data.includes(name)) return document.createElement(name);
                return new (eval(name)); 
            })();
             if(typeof innerHTML == "string") element.innerHTML = innerHTML;
             else element.append(innerHTML);
            element.innerHTML = innerHTML
            return element;
        })('Span', 'A paragraph of text.')
        ;


console.log(0-10)0-